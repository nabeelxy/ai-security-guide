# Docusaurus — lock file IS committed for reproducible CI builds
site/node_modules/
site/.docusaurus/
site/build/

# Downloaded papers (large binaries — tracked via manifest.json instead)
raw/papers/

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
