/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  // Fully autogenerated — ordering and labels controlled via _category_.json
  // files placed in each content directory.
  mainSidebar: [{ type: 'autogenerated', dirName: '.' }],
};

module.exports = sidebars;
